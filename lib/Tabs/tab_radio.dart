import 'package:flutter/material.dart';

class Tab_Radio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        // child: Column(
        //   children: [
        //     Image.asset('asset/images/radio.png'),
        //     ListView.builder(
        //       itemBuilder: (context, index) => Text('gg'),
        //       itemCount: 5,
        //     )
        //   ],
        // ),
        );
  }
}
